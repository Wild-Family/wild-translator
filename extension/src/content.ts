// Minimal content script: send selected text to extension UI via storage (optional future)
// For now, it only exposes a helper through window for debugging.

export {};

// Example future hook: you could listen for a keyboard shortcut and send selection.
// Keeping this file minimal as requested.
