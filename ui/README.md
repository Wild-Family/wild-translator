This directory contains the extension UI built with static HTML, CSS, and DOM-focused TypeScript.

Files under `ui/static/` are copied directly into `extension-dist/ui/`.
Files under `ui/src/` are compiled by the root TypeScript build and copied into `extension-dist/ui/src/`.
